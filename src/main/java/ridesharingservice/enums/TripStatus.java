package ridesharingservice.enums;

/**
 * just an attribute of Trip class, changes along with states
 */
public enum TripStatus {
    REQUESTED,
    ASSIGNED,
    IN_PROGRESS,
    COMPLETED,
    CANCELLED
}
